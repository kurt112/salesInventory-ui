import { makeStyles } from "@material-ui/styles"

const style = makeStyles((theme) => ({
    header: {
        marginTop: 10,

    },
    card: {
        textAlign: "center",
        color: 'white',
        marginBottom: 10,
        width: 10
    },
    icons: {
        fontSize: 60
    }
}))


export default style;
